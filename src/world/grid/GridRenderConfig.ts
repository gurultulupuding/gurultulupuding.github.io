export type GridRenderConfig = {
  showRiverTiles: boolean;
  showBlockedTiles: boolean;
};

export const DEFAULT_GRID_RENDER_CONFIG: GridRenderConfig = {
  showRiverTiles: false,
  showBlockedTiles: false,
};